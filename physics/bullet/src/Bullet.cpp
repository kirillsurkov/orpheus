#include "physics/bullet/Bullet.hpp"

#include <iostream>

namespace orpheus::physics::bullet {
    void Bullet::init(float timestep) {
    }

    ID Bullet::addBody(const Description& description) {

    }

    void Bullet::update(float delta) {

    }

    void Bullet::apply() {

    }

    void Bullet::setLinearVelocity(ID id, const math::Vector3& dir) {

    }

    math::Vector3 Bullet::getLinearVelocity(ID id) const {

    }

    State Bullet::getState(ID id) const {

    }
}
